# Pull Request

## What & why

<!-- Describe what this changes and why. One or two paragraphs is plenty. -->

## Linked issue

<!-- "Fixes #123", "Refs #456", or "n/a" if there isn't one. -->

## Testing done

<!-- What did you do to verify this works? e.g. `npm run build`, `npm test`,
`npm run lint`, manual run of `termwhat ...`. Paste relevant output if it
helps reviewers. -->

## Checklist

- [ ] `npm run build` passes
- [ ] Tests added/updated and `npm test` passes
- [ ] `npm run lint` passes
- [ ] Docs updated where relevant
- [ ] No API keys, personal hostnames, or other secrets in this diff
- [ ] This change does **not** cause `termwhat` to execute suggested commands
      (the suggestion-only invariant is preserved)